TODO list for package koRpus:

 - keep stopword and stem information in kRp.corp.freq objects
 - find global solution for corp.rm.class="nonpunct" in several internal functions (it's a bit scattered...)
